{
  "name": "trilith-ask-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backing the ask-the-corpus chat on the Trilith competitive intelligence page.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put ANTHROPIC_API_KEY",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
